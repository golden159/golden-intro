# Golden Intro 公开内容接入设计规格

## 状态

- 高层方案：已确认，采用方案 A
- 本规格：待用户审核
- 实现范围：现有单文件静态页面，不引入框架或构建依赖

## 背景

个人资料已经整理到 `content/`，头像已经放入 `assets/avatar-public.jpg`，但当前首页 `index.html` 仍显示 Golden 图形、项目空状态和联系方式占位语。资料文件与页面之间还没有接入关系，因此此前部署后看不到新内容。

## 目标

将已确认可公开的个人资料接入现有首页，使访客能够直接看到：

1. 真实头像和个人定位。
2. 一段简洁的个人简介。
3. 两个已整理的项目及其可核验指标。
4. 两个邮箱、GitHub、X/Twitter 和公开微信号。
5. 保持当前页面的视觉语言、粒子效果、双模式导航和移动端布局。

## 非目标

- 不引入 React、Markdown 运行时解析器或其他构建系统。
- 不为尚未提供的项目仓库、Demo、截图或博客地址制造链接。
- 不在页面展示手机号、GPA 或“专业前 10%”。
- 不在本次改动中处理 Vercel 登录保护策略；部署验证单独确认。

## 已确认的信息边界

### 个人信息

- 页面身份：许泽升 / golden
- 学校与专业：深圳大学，光电信息科学与工程本科生
- 方向：AI Agent、深度学习、计算机视觉、低层视觉、光学测量、计算成像
- 简介采用 `content/profile.md` 中的候选文案，并根据首页空间做轻量排版调整

### 联系方式

- `1623206759@qq.com`
- `xzs13549929558@gmail.com`
- GitHub：`https://github.com/golden159`
- X/Twitter：`https://x.com/oldenG562897`
- 微信：纯文本显示 `golden-xzs`
- Blog：显示为未提供地址的非链接状态，或在页面中省略；不得生成不可用的可点击链接

## 页面接入设计

### 1. Hero

- 将现有 Golden 图形替换为 `assets/avatar-public.jpg`。
- 保留当前头像外层蓝图框、粒子画布和扫描线效果。
- 头像使用等比例适配，避免拉伸；在窄屏下保持主体可见。
- 更新图片 `alt` 文本为描述性公开信息，不暴露额外隐私。
- 将社交分享图像元数据同步到公开头像，避免分享卡片继续使用旧的 Golden 图形。

### 2. About

保留现有 About 区域的结构和模式切换，将占位内容替换为个人简介，重点表达“光学系统 + 数学模型 + 深度学习”的交叉方向。文案只使用简历和已确认资料，不补充未经确认的奖项、排名或工作经历。

### 3. Work

将当前“项目正在整理，暂未公开”的空状态替换为两个项目卡片：

#### 大深径比微孔参数光学无损测量系统

- 时间：2025.03–2025.09
- 场景：全国大学生光电设计竞赛
- 角色：系统总体设计与核心算法
- 技术摘要：PZT 精密位移台、USB3 工业相机、光学成像与图像处理
- 公开指标：检测时间 180s 降至 70s；平均绝对误差 3.28% 降至 1.36%；最大误差 7.21% 降至 4.40%

#### BetaPPM 物理散射参数感知与去雾推理资源自适应系统

- 时间：2025.09–至今
- 角色：核心模型与推理调度
- 技术摘要：大气散射模型、BetaHazeDataset、ResNet50 双头分类/回归、自适应采样
- 公开指标：分类准确率 80% 提升至 92%；F1 由 0.654 提升至 0.753；RTTS 上 MUSIQ 提升 1.03、PAQ2PIQ 提升 0.18

项目卡片只展示已有资料和指标。仓库、Demo、论文和图片链接待资料补齐后再追加。

### 4. Contact

- 邮箱使用 `mailto:` 链接。
- GitHub 和 X/Twitter 使用外部链接，并添加新窗口打开及安全的 `rel` 属性。
- 微信号以普通文本展示，明确标注为公开微信号，不调用二维码或添加虚假的跳转。
- 删除“联系方式整理中”和“LOCAL ONLY”等与公开页面状态冲突的占位语。
- 手机号不出现在 HTML、可见文本、链接、`data-*` 属性或页面脚本中。

## 实现约束

### 静态内容同步

`content/*.md` 继续作为资料源和后续维护入口；由于当前站点没有构建步骤，本次将已确认文案静态同步到 `index.html`。后续若资料规模扩大，再单独评估 Markdown 构建方案，避免为本次内容接入引入不必要的运行时依赖。

### 现有交互保持不变

- 保留 resume/social 两种模式及其导航切换。
- 保留粒子头像的加载、鼠标交互、移动端降级和 reduced-motion 行为。
- 项目卡片在桌面端并排或网格排列，在移动端纵向排列。
- 所有新增可交互元素具备键盘焦点样式和可读的链接文本。

## 验收标准

1. 首页不再出现项目整理空状态、联系方式整理中或 `LOCAL ONLY` 占位语。
2. Hero 使用 `assets/avatar-public.jpg`，桌面端和移动端均不拉伸、不溢出。
3. About 能看到已确认的个人简介和方向关键词。
4. Work 能看到两个项目名称、时间、角色、技术摘要和指标，且没有虚构链接。
5. 两个邮箱、GitHub、X/Twitter 可点击；微信号显示为 `golden-xzs`。
6. 手机号、GPA 和专业排名不出现在页面内容中。
7. 现有 Node 测试与新增内容断言全部通过，静态资源路径可访问。
8. 推送到现有 `agent/restore-golden-intro` 分支后只触发一次 GitHub 关联的 Vercel Preview；不再同时手动创建重复部署。

## 验证方案

实现前先补充针对页面内容和隐私边界的测试，使测试先失败，再完成最小页面改动。实现后执行：

- `node --test tests/*.test.cjs`
- 静态搜索确认旧占位文案和手机号均已移除
- 本地静态 HTTP 服务检查 `index.html`、头像和 favicon 均返回成功
- 检查 Git 状态、提交内容和远程分支，确保只包含本次页面接入相关文件

## 后续可扩展项

以下内容不阻塞本次接入，待用户补充后再做：

- 两个项目的 GitHub 仓库或 Demo 链接
- 项目截图、系统架构图和结果对比图
- Blog 地址
- 是否公开 GPA、排名或更详细的教育经历
- 是否解除或调整 Vercel Preview 的登录保护
